interface GlobalVariable {
    centerToDestroy: number,
    offAllSounds: boolean,
}

let GlobalVariable: GlobalVariable = {
    centerToDestroy: -1250,
    offAllSounds: false,
};
export default GlobalVariable;